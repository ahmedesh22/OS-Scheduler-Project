# Remove git completely
sudo apt-get remove --purge git

# Reinstall git
sudo apt-get update
sudo apt-get install git
sudo apt-get install git-https
sudo yum install git-https
git --version
sudo apt update
sudo apt install -y \
    build-essential \
    libcurl4-openssl-dev \
    libssl-dev \
    libexpat1-dev \
    gettext \
    unzip \
    wget \
    zlib1g-dev \
    gcc \
    make \
    perl
sudo apt-get install --reinstall git