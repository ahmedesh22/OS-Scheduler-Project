// here we should have a priqueue that hold the processes according to their run times
#pragma once
#include "Priqueue.h"
#include "headers.h"

// struct priqueue* Processes_PriQueue = (struct priqueue* )malloc(sizeof(struct priqueue));

void SJF()
{
    
}